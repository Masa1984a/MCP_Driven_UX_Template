{
  "db": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME}",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}"
  },
  "srcDir": "api/src/sql",
  "glob": "**/*.sql",
  "prettier": true,
  "failOnError": true
}
